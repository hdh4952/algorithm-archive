#include <bits/stdc++.h>
using namespace std;

void solve() {
  int P, D; cin >> P >> D;
  cout << D << " " << 5 << "\n";
}

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  cout.tie(0);

  int N; cin >> N;
  while (N--) {
    solve();
  }
}
